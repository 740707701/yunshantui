define(['./controllers'], function(controllers) {
    controllers.controller('PublicityCtrl', ['$scope',
        function($scope) {
            console.log('PublicityCtrl page');
        }
    ]);
});